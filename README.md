# RefreshBackGround
Every 5 minutes a image from NOAA satellite images and saved to a directory on your local hardrive.
Sets the dowmloaded image to the desktop background. 
The image is refreshId every 5 minutes.

